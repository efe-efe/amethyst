export const enum Custom_ActionModes {
    START = 0,
    STOP = 1,
}

export const enum Custom_ActionTypes {
    MOVEMENT = 0,
    ABILITY = 1,
}

export const enum CUSTOM_GameState {
    CUSTOM_GAMESTATE_NONE = 0,
    CUSTOM_GAMESTATE_GAMEMODE_SELECTION = 1,
    CUSTOM_GAMESTATE_PRE_MATCH = 2,
    CUSTOM_GAMESTATE_MATCH = 3,
    CUSTOM_GAMESTATE_POST_MATCH = 4,
}